version: 1.0
runtime: python3
build:
  commands:
    pre-build:
      - python --version
      - pip install -r requirements.txt
run:
  command: python -m gunicorn --bind=0.0.0.0:8000 app:app
  network:
    port: 8000
    env: PORT
  env:
    - name: PORT
      value: "8000"
